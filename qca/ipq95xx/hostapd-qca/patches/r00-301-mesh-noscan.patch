Index: hostapd-2022-09-19-01944c09/wpa_supplicant/config.c
===================================================================
--- hostapd-2022-09-19-01944c09.orig/wpa_supplicant/config.c
+++ hostapd-2022-09-19-01944c09/wpa_supplicant/config.c
@@ -2714,6 +2714,7 @@ static const struct parse_data ssid_fiel
 #else /* CONFIG_MESH */
 	{ INT_RANGE(mode, 0, 4) },
 #endif /* CONFIG_MESH */
+	{ INT_RANGE(noscan, 0, 1) },
 	{ INT_RANGE(proactive_key_caching, 0, 1) },
 	{ INT_RANGE(disabled, 0, 2) },
 	{ STR(id_str) },
Index: hostapd-2022-09-19-01944c09/wpa_supplicant/config_file.c
===================================================================
--- hostapd-2022-09-19-01944c09.orig/wpa_supplicant/config_file.c
+++ hostapd-2022-09-19-01944c09/wpa_supplicant/config_file.c
@@ -769,6 +769,7 @@ static void wpa_config_write_network(FIL
 #endif /* IEEE8021X_EAPOL */
 	INT(mode);
 	INT(no_auto_peer);
+	INT(noscan);
 	INT(mesh_fwding);
 	INT(frequency);
 	INT(enable_edmg);
Index: hostapd-2022-09-19-01944c09/wpa_supplicant/config_ssid.h
===================================================================
--- hostapd-2022-09-19-01944c09.orig/wpa_supplicant/config_ssid.h
+++ hostapd-2022-09-19-01944c09/wpa_supplicant/config_ssid.h
@@ -1004,6 +1004,8 @@ struct wpa_ssid {
 	 */
 	int no_auto_peer;
 
+	int noscan;
+
 	/**
 	 * mesh_rssi_threshold - Set mesh parameter mesh_rssi_threshold (dBm)
 	 *
Index: hostapd-2022-09-19-01944c09/wpa_supplicant/mesh.c
===================================================================
--- hostapd-2022-09-19-01944c09.orig/wpa_supplicant/mesh.c
+++ hostapd-2022-09-19-01944c09/wpa_supplicant/mesh.c
@@ -553,6 +553,8 @@ static int wpa_supplicant_mesh_init(stru
 			   frequency);
 		goto out_free;
 	}
+	if (ssid->noscan)
+		conf->noscan = 1;
 
 	if (ssid->mesh_basic_rates == NULL) {
 		/*
Index: hostapd-2022-09-19-01944c09/wpa_supplicant/wpa_supplicant.c
===================================================================
--- hostapd-2022-09-19-01944c09.orig/wpa_supplicant/wpa_supplicant.c
+++ hostapd-2022-09-19-01944c09/wpa_supplicant/wpa_supplicant.c
@@ -2650,8 +2650,8 @@ void ibss_mesh_setup_freq(struct wpa_sup
 	int ieee80211_mode = wpas_mode_to_ieee80211_mode(ssid->mode);
 	enum hostapd_hw_mode hw_mode;
 	struct hostapd_hw_modes *mode = NULL;
-	int ht40plus[] = { 36, 44, 52, 60, 100, 108, 116, 124, 132, 149, 157,
-			   165, 173, 184, 192 };
+	int ht40plus[] = { 1, 2, 3, 4, 5, 6, 36, 44, 52, 60, 100, 108, 116, 124, 132, 149, 157,
+			   184, 192 };
 	/* bw_80_160 array members are 80MHz start freq, 80MHz end freq and so on
 	 */
 	unsigned int bw_80_160[] = { 5180, 5240, 5260, 5320,
@@ -2671,7 +2671,7 @@ void ibss_mesh_setup_freq(struct wpa_sup
 				  6595, 6895, 6755, 7055};
 	struct hostapd_channel_data *pri_chan = NULL, *sec_chan = NULL;
 	u8 channel,  chan_80mhz;
-	int i, chan_idx, ht40 = -1, res, obss_scan = 1;
+	int i, chan_idx, ht40 = -1, res, obss_scan = !(ssid->noscan);
 	unsigned int j, k;
 	struct hostapd_freq_params vht_freq;
 	int chwidth, seg0, seg1;
